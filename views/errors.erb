<h4>Oops there were some errors:</h4>

<ul>
  <% @bands.errors().full_messages().each do |message| %>
  <li><%= message %></li>
  <% end %>
</ul>
<h5><a href='/' > Return to main page to try again</a></h5>
